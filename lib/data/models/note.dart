class Note {
  final String name;
  final int octave;

  // 2. Constructor
  // This is a special function for creating an instance of the class.
  // This "primary constructor" syntax directly assigns arguments to the final fields.
  Note(this.name, this.octave);
}
